<h3>Edit Page</h3>

<%= form_for(@pet) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :species %>
  <%= f.select(:species, options_for_select(["Dog", "Cat", "Fish"])) %>

  <%= f.label :age %>
  <%= f.number_field :age %>

  <%= f.submit %>

<% end %>
